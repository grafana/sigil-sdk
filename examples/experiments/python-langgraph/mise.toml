[env]
PYTHONPATH = ""
PYTHONHOME = ""

[tasks.sync]
description = "Install deps into the local venv"
run = "uv sync"

[tasks.experiment]
description = "Run the LangGraph dataset experiment against Sigil"
run = "uv run python -m app.run_experiment"
